{
  "generated_at": "2026-08-13T14:36:01.448304+00:00",
  "status": "PASS",
  "passes": [
    "required file: site/index.html",
    "required file: site/404.html",
    "required file: site/it-services/it-support/index.html",
    "required file: site/.well-known/security.txt",
    "required file: Start-Staging.ps1",
    "required file: tools/dev/_server.py",
    "required file: tools/build_production.py",
    "required file: tools/asset_minifier.py",
    "required file: tools/Apply-Update.ps1",
    "required file: tools/Build-Production.ps1",
    "required file: tools/Smoke-Test-Production.ps1",
    "required file: deploy/nginx-security-headers.conf",
    "required file: deploy/nginx-site.conf.example",
    "required file: deploy/activate-release.sh",
    "required file: docs/BROWSER-SUPPORT.md",
    "required file: docs/DEPLOYMENT.md",
    "required file: docs/FRAMEWORK-HARDENING.md",
    "no confidential DOCX files live under reference/",
    "full source-scrape archive is outside the working project",
    "Git repository is initialised",
    ".gitignore excludes staging/runtime/",
    ".gitignore excludes staging/backups/",
    ".gitignore excludes dist/",
    ".gitignore excludes **/__pycache__/",
    ".gitignore excludes *.pyc",
    "baseline Git commit exists",
    "homepage hero is video-only with no static poster layer",
    "standalone oversized Staple logo interlude is absent",
    "homepage hero CSS has no static liquid-wave image",
    "navigation ribbon uses transparent black glass",
    "HTML structural lint passes (17 pages)",
    "no broken internal links (17 pages scanned)",
    "no missing local HTML assets",
    "canonical/Open Graph/Twitter metadata is present and placeholders are noindex",
    "homepage ProfessionalService JSON-LD is present and parseable",
    "liquidGL is fully self-hosted with the pinned SHA-256",
    "contained liquidGL surfaces are enabled by default",
    "liquidGL persistent target uses specular:false",
    "liquidGL loader has no CDN fallback",
    "liquid-enhance.js loads exactly on pages with a liquidGL target",
    "liquidGL is used selectively across the homepage and service pages",
    "each liquidGL page contains one to three small contained targets",
    "every liquidGL target declares an explicit local snapshot region",
    "no nav shell or nav pill is a liquidGL target",
    "no hero shell/stage is itself a liquidGL target",
    "one-word staging commands are forced to an array (System.Char crash guard)",
    "staging command token is explicitly cast to String before case normalization",
    "staging command parsing is inside the per-command try/catch",
    "local dev server disables static asset caching",
    "exactly one dev server exists",
    "no Python cache artefacts are present",
    "no version-stacked documentation files remain",
    "one current file exists for each core documentation topic",
    "security headers are real nginx add_header directives",
    "HSTS remains explicitly disabled until HTTPS validation",
    "timestamped release/current-symlink rollback strategy is documented",
    ".well-known/security.txt exists",
    "production build completes cleanly",
    "dist/ is rebuilt cleanly with no stale files",
    "build generates robots.txt and sitemap.xml",
    "generated sitemap.xml is valid XML",
    "production JavaScript is genuinely minified",
    "production CSS is genuinely minified",
    "old pseudo-minifier functions are removed from the builder",
    "dist/ contains deployable site content only",
    "local dev server serves homepage and all IT Services dropdown pages",
    "local dev server serves the custom 404 with HTTP 404 status"
  ],
  "failures": [],
  "metrics": {
    "git_commits": 8,
    "source_html_pages": 17,
    "real_pages": 6,
    "placeholder_pages": 10,
    "build_stdout": [
      "Production build created: /mnt/data/stapleit-framework-black-gloss/dist",
      "[PASS] Clean dist/ generated from site/ only.",
      "[PASS] liquidGL vendor SHA-256 verified.",
      "[PASS] Parser/token-aware asset minification: 84233 -> 79261 bytes (5.9% smaller).",
      "[PASS] robots.txt and sitemap.xml generated (6 indexable route(s))."
    ]
  }
}
